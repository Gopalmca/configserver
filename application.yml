
eureka:
  client:
    fetchRegistry: true
    instance:
      preferIpAddress: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
      
 datasource:
    password: root
    url: jdbc:mysql://localhost:3306/u_bank
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect      
      
      
